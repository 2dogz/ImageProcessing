version: '3.8'
services:
    postgres:
        environment:
            - POSTGRES_USER=user
            - POSTGRES_PASSWORD=password123
        container_name: db-my
        ports:
            - '5432:5432'
        restart: always
        image: postgres
